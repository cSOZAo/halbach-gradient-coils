# Runtime dependencies of coilgen/ + gui/ + the CLI entry points.
#
# pyCoilGen is NOT listed: it is resolved from a sibling checkout by adding its
# parent directory to sys.path (see coilgen/__init__.py and the README).
# The GUI uses tkinter, which ships with CPython.
#
# This file used to be a full `pip freeze` of a development machine (~170
# packages: jupyter, aiohttp, yfinance, pygame, PyQt6, ...). None of that is
# imported by this project, and it pulled in a large vulnerable surface, so the
# list is now restricted to what the code actually imports.

manifold3d==3.5.2
matplotlib==3.10.3
numpy==1.26.4
# matplotlib image backend; pinned explicitly to a version without the
# known Pillow RCE / buffer-overflow advisories.
Pillow==12.3.0
scipy==1.15.3
trimesh==3.23.5
