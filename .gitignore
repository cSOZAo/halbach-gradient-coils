# Simulation outputs (STLs, pickles, plots) — keep out of git
/resultados/
/standalone/output/

# Python / logs
__pycache__/
*.py[cod]
*.log

# Local backups (not source of truth)
_backup*/
_backups*/
*_BACKUP/
